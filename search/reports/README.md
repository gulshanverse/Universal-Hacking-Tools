# Reporting Extension Point

Generated health data is stored in [`../../generated/knowledge-health.json`](../../generated/knowledge-health.json), while discovery and search responses are available as stable Python return objects and JSON CLI output. Future reports should consume normalized artifacts, preserve deterministic ordering, and avoid exposing sensitive repository content.
