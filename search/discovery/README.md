# Discovery Extension Point

The reusable discovery implementation lives in [`../engine/discovery_engine.py`](../engine/discovery_engine.py). This directory is reserved for future discovery adapters and report formatters; current behavior remains local, deterministic, bounded, and graph-backed.
