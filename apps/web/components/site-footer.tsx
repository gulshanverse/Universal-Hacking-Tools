/* Signal Archive: concise repository provenance and an explicit safety boundary. */
import Link from "next/link";

export function SiteFooter() {
  return <footer className="border-t hairline bg-[var(--ink)] text-[var(--paper)]"><div className="mx-auto grid max-w-7xl gap-8 px-4 py-10 sm:px-6 md:grid-cols-[1.5fr_1fr_1fr]"><div><p className="mono text-[10px] tracking-[.18em] text-[var(--copper)]">KNOWLEDGE BASE / PHASE 7</p><h2 className="display mt-2 text-2xl">An evidence-aware archive for authorized practice.</h2><p className="mt-3 max-w-md text-sm leading-6 text-[var(--mist)]">This platform surfaces generated repository contracts. It does not perform target scanning, arbitrary command execution, or remote telemetry.</p></div><div><p className="mono text-[10px] tracking-[.16em] text-[var(--mist)]">EXPLORE</p><div className="mt-3 grid gap-2 text-sm">{[["Knowledge health","/about/health"],["Contributor guide","/contribute"],["Learning paths","/learning-paths"],["Local labs","/labs"]].map(([label,href]) => <Link className="hover:text-[var(--copper)]" href={href} key={href}>{label}</Link>)}</div></div><div><p className="mono text-[10px] tracking-[.16em] text-[var(--mist)]">BOUNDARY</p><p className="mt-3 text-sm leading-6 text-[var(--mist)]">Lab fixtures are local, isolated, disposable, and explicitly authorized. Review and content changes remain Git-based.</p></div></div></footer>;
}
