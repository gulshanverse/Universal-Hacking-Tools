/* Signal Archive learning: selected goals are validated against generated learning paths. */
import { LearningView } from "../../../components/dashboard-views";
export default function DashboardLearningPage() { return <LearningView/>; }
