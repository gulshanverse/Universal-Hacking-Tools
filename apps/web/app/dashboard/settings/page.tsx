/* Signal Archive settings: user-controlled private preference and deletion controls. */
import { SettingsView } from "../../../components/dashboard-views";
export default function DashboardSettingsPage() { return <SettingsView/>; }
