/* Signal Archive labs: owner-only summaries, never raw lab evidence. */
import { LabsView } from "../../../components/dashboard-views";
export default function DashboardLabsPage() { return <LabsView/>; }
