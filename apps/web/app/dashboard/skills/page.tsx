/* Signal Archive skills: deterministic estimates, never public credentials. */
import { SkillsView } from "../../../components/dashboard-views";
export default function DashboardSkillsPage() { return <SkillsView/>; }
