/* Signal Archive dashboard: owner-scoped state references generated knowledge only. */
import { OverviewView } from "../../components/dashboard-views";
export default function DashboardPage() { return <OverviewView/>; }
