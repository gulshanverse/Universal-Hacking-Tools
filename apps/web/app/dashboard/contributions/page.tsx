/* Signal Archive private workspace: structured contribution forms and restrained status history, never a canonical editor. */
import { DashboardShell } from "../../../components/dashboard-shell";
import { ContributionWorkspace } from "../../../components/community-workspaces";
export default function ContributionsPage() { return <DashboardShell title="Contributions"><ContributionWorkspace /></DashboardShell>; }
