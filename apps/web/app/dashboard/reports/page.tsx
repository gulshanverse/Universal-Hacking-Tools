/* Signal Archive private dashboard: owner-only concern reporting with a dedicated private security path. */
import { DashboardShell } from "../../../components/dashboard-shell";
import { ReportWorkspace } from "../../../components/community-workspaces";
export default function ReportsPage() { return <DashboardShell title="Private reports"><ReportWorkspace /></DashboardShell>; }
