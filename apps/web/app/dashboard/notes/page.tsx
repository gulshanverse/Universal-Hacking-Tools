/* Signal Archive notes: private plain text, never rendered as HTML or Markdown. */
import { NotesView } from "../../../components/dashboard-views";
export default function DashboardNotesPage() { return <NotesView/>; }
