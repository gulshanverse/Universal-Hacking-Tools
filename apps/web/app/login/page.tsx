/* Signal Archive account entry: protected by server-side opaque sessions. */
import { AccountForm } from "../../components/account-form";
export default function LoginPage() { return <AccountForm mode="login"/>; }
