/* Signal Archive contribution record: published history only, deliberately separated from current canonical knowledge. */
import { notFound } from "next/navigation";
import Link from "next/link";
import { CommunityContribution, api } from "../../../../lib/api";
import { pageMetadata } from "../../../../lib/metadata";
export async function generateMetadata({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return pageMetadata("Published community contribution", "Approved community contribution history; canonical knowledge remains Git generated.", `/community/contributions/${id}`); }
export default async function PublishedContributionPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; let item: CommunityContribution; try { item = await api<CommunityContribution>(`/community/contributions/${encodeURIComponent(id)}`); } catch { notFound(); } return <section className="mx-auto max-w-4xl px-4 py-12 sm:px-6"><Link href="/community" className="text-sm text-[var(--teal)] underline underline-offset-4">Community</Link><p className="mono mt-7 text-[10px] tracking-[.16em] text-[var(--copper)]">PUBLISHED CONTRIBUTION HISTORY</p><h1 className="display mt-2 text-5xl font-bold">{item.title}</h1><p className="mt-3 text-sm text-[var(--slate)]">{item.type} · {item.author || "Former Contributor"} · Published {item.published_at ? new Intl.DateTimeFormat("en", { dateStyle: "medium" }).format(new Date(item.published_at)) : "through reviewed repository history"}</p><p className="mt-7 text-lg leading-8 text-[var(--slate)]">{item.description}</p><aside className="mt-9 border-l-2 border-[var(--teal)] bg-[var(--paper-dark)] px-4 py-3 text-sm leading-6 text-[var(--ink)]">This is an application attribution record. Canonical knowledge remains the repository’s reviewed Markdown/YAML and generated contracts.</aside>{item.github_pr_url && <a className="mt-7 inline-block text-sm text-[var(--teal)] underline underline-offset-4" href={item.github_pr_url} rel="noreferrer">View confirmed pull request</a>}</section>; }
