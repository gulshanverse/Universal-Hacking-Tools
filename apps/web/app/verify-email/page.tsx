/* Signal Archive verification: one-time email token is submitted only to the API. */
import { AccountForm } from "../../components/account-form";
export default function VerifyEmailPage() { return <AccountForm mode="verify"/>; }
