/* Signal Archive registration: requests a private account without public profiles. */
import { AccountForm } from "../../components/account-form";
export default function RegisterPage() { return <AccountForm mode="register"/>; }
