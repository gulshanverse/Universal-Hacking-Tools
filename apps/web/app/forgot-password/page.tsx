/* Signal Archive recovery: generic response prevents account enumeration. */
import { AccountForm } from "../../components/account-form";
export default function ForgotPasswordPage() { return <AccountForm mode="forgot"/>; }
