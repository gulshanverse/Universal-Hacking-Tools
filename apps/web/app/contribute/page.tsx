/* Signal Archive: read-only contribution guidance keeps source changes Git-based and avoids online editing. */
import { pageMetadata } from "../../lib/metadata";
export const metadata=pageMetadata("Contribute","Read-only contribution, verification, source, relationship, and lab review guidance.","/contribute");
export default function ContributePage(){ const items=[["Content","Add or improve Markdown and YAML source records through reviewed Git changes."],["Verification","Use controlled statuses, confidence, methods, and evidence without falsely upgrading facts."],["Sources","Prefer authoritative sources and preserve incomplete metadata as review findings rather than inventing it."],["Relationships","Review prerequisites, typed graph relationships, and visible orphan or gap reports."],["Labs","Classify execution mode and require isolated, local synthetic fixtures before proposing an executable definition."],["Community","Follow the Code of Conduct, Security Policy, and pull-request templates in the repository."]]; return <section className="mx-auto max-w-5xl px-4 py-12 sm:px-6"><p className="mono text-[10px] tracking-[.16em] text-[var(--copper)]">CONTRIBUTE / READ ONLY</p><h1 className="display mt-2 text-5xl font-bold">Improve the archive through reviewable change.</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-muted">Phase 7 exposes contribution guidance, not online editing. Content modification remains Git-based so sources, safety, verification, and relationships can be reviewed in context.</p><div className="mt-9 grid gap-4 md:grid-cols-2">{items.map(([title,text],index)=><article className="border hairline bg-white p-5" key={title}><p className="mono text-[10px] text-[var(--copper)]">0{index+1}</p><h2 className="display mt-2 text-2xl">{title}</h2><p className="mt-2 text-sm leading-6 text-muted">{text}</p></article>)}</div></section>; }
